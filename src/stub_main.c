/*
 * src/stub_main.c — placeholder main() for Phase 1 linkability.
 *
 * Plan 2 replaces this with src/app.c (the full Toolbox app shell with
 * InitGraf, WaitNextEvent, menu bar, etc.). For Phase 1 we only need
 * ArmadilloEditor.APPL to link, so this provides the minimum main
 * symbol Retro68's startup code demands.
 *
 * This file will be deleted when src/app.c lands.
 */
int main(void)
{
    return 0;
}
